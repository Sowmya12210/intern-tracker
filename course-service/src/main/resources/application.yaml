spring:
  application:
    name: course-service
  datasource:
    password: iiitn123
    url: jdbc:mysql:///course-service?cloudSqlInstance=fluted-factor-438905-d2:us-west1:intern-tracker&socketFactory=com.google.cloud.sql.mysql.SocketFactory

    username: root
  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    serverUrl:
      defaultZone: http://localhost:8761/eureka
  instance:
    hostname: localhost
    prefer-ip-address: true

server:
  port: 8083
